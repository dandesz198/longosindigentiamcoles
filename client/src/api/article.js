import api from './api'

// export async function removeFeature({ }) {
//   const { data } = await api.delete(
//     `/stories/${storyId}/slides/${slideId}/features/${featureId}`
//   )
//   return data
// }
